name: calculadora_imc
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=2.15.0 <3.0.0'

dependencies:
  flutter:
    sdk: flutter
  hive_flutter: 1.1.0
  hive: ^2.0.4
  path_provider: ^2.0.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
